#!/bin/sh
echo "🔨 Running build check before push..."
npm run build 2>&1
if [ $? -ne 0 ]; then
  echo "❌ Build failed — push blocked. Fix errors before pushing."
  exit 1
fi
echo "✅ Build passed — pushing."
